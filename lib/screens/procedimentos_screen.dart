import 'package:flutter/material.dart';

class ProcedimentosScreen extends StatelessWidget {
  const ProcedimentosScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Procedimentos')),
      body: const Center(child: Text('Tela de Procedimentos e Manejo\n(Aguardando atualizações futuras)', textAlign: TextAlign.center)),
    );
  }
}